import type { MetadataRoute } from "next";
import { articles, caseStudies, services, siteConfig } from "@/data/site";
export default function sitemap():MetadataRoute.Sitemap{const base=["","/about","/services","/work","/case-studies","/pricing","/insights","/contact","/book-demo","/careers","/privacy-policy","/terms-and-conditions"].map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date("2026-06-25"),changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:path==="/services"||path==="/contact"?0.9:0.7}));return [...base,...services.map(s=>({url:`${siteConfig.url}/services/${s.slug}`,lastModified:new Date("2026-06-25"),changeFrequency:"monthly" as const,priority:.8})),...caseStudies.map(c=>({url:`${siteConfig.url}/case-studies/${c.slug}`,lastModified:new Date("2026-06-25"),changeFrequency:"monthly" as const,priority:.7})),...articles.map(a=>({url:`${siteConfig.url}/insights/${a.slug}`,lastModified:new Date("2026-06-25"),changeFrequency:"monthly" as const,priority:.6}))]}
