import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { CheckCircle2 } from "lucide-react";
import { Faq } from "@/components/faq";
import { FinalCta } from "@/components/final-cta";
import { JsonLd } from "@/components/json-ld";
import { Reveal } from "@/components/reveal";
import { Container, PageHero, PrimaryButton, SecondaryButton, SectionHeading } from "@/components/ui";
import { processSteps, services, siteConfig } from "@/data/site";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return{};return{title:`${s.title} in Rajkot`,description:s.summary,keywords:s.keywords,alternates:{canonical:`/services/${s.slug}`},openGraph:{title:`${s.title} | DiGi Trend`,description:s.summary,url:`${siteConfig.url}/services/${s.slug}`}}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const faqs=[{q:`What is included in ${s.title}?`,a:`Every project is scoped to your business needs. Typical deliverables include ${s.deliverables.slice(0,4).join(", ")} and launch support.`},{q:"How long does a project take?",a:"Timelines depend on scope and feedback cycles. After discovery, we provide clear stages, milestones and an estimated delivery schedule."},{q:"Can this service be combined with other DiGi Trend services?",a:"Yes. Most growth projects work best when connected with complementary capabilities such as branding, website, content, marketing or automation."},{q:"How do we begin?",a:"Book a free consultation. We will review your current position, priorities and recommend a practical starting scope."}];return <><JsonLd data={[{"@context":"https://schema.org","@type":"Service",name:s.title,description:s.summary,provider:{"@type":"Organization",name:siteConfig.name,url:siteConfig.url},areaServed:"India",url:`${siteConfig.url}/services/${s.slug}`},{"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:siteConfig.url},{"@type":"ListItem",position:2,name:"Services",item:`${siteConfig.url}/services`},{"@type":"ListItem",position:3,name:s.title,item:`${siteConfig.url}/services/${s.slug}`}]}]}/><PageHero eyebrow={s.shortTitle} title={<>{s.hero.split(" ").slice(0,-3).join(" ")} <span className="text-red-300">{s.hero.split(" ").slice(-3).join(" ")}</span></>} description={s.summary}><PrimaryButton href="/book-demo">Discuss This Service</PrimaryButton><SecondaryButton href="/work" inverse>View Related Work</SecondaryButton></PageHero><section className="bg-white py-20 sm:py-28"><Container><div className="grid gap-12 lg:grid-cols-[.85fr_1.15fr] lg:gap-20"><Reveal><p className="text-xs font-black uppercase tracking-[.2em] text-brand-red">The challenge</p><h2 className="font-heading mt-4 text-3xl font-black leading-[1.05] tracking-[-.05em] text-brand-navy sm:text-5xl">{s.problem}</h2></Reveal><Reveal delay={100}><p className="text-lg leading-8 text-slate-600">Our approach combines clear strategy, premium design standards and practical execution. The result is not just a deliverable — it is a stronger part of your brand growth system.</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{s.benefits.map(x=><div key={x} className="flex items-center gap-3 rounded-2xl bg-brand-offwhite p-4 text-sm font-bold text-brand-navy"><CheckCircle2 className="size-5 text-brand-red"/>{x}</div>)}</div></Reveal></div></Container></section><section className="bg-brand-offwhite py-20 sm:py-28"><Container><SectionHeading eyebrow="What we provide" title={<>A clear scope built around <span className="text-brand-red">business impact.</span></>} description="Deliverables are adapted to your exact goals, but a typical engagement can include:"/><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{s.deliverables.map((x,i)=><Reveal key={x} delay={(i%3)*60}><div className="rounded-[24px] bg-white p-6"><p className="text-[10px] font-black uppercase tracking-[.18em] text-brand-red">0{i+1}</p><h3 className="font-heading mt-3 text-lg font-black text-brand-navy">{x}</h3></div></Reveal>)}</div></Container></section><section className="bg-brand-navy py-20 text-white sm:py-28"><Container><SectionHeading eyebrow="Our process" title={<>From discovery to <span className="text-red-300">confident launch.</span></>} light/><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{processSteps.map((p,i)=><div key={p.title} className="rounded-[24px] border border-white/10 bg-white/5 p-6"><p className="text-[10px] font-black uppercase tracking-[.18em] text-red-300">Step {i+1}</p><h3 className="font-heading mt-3 text-xl font-black">{p.title}</h3><p className="mt-3 text-sm leading-6 text-white/50">{p.text}</p></div>)}</div></Container></section><section className="bg-white py-20 sm:py-28"><Container><div className="grid gap-10 lg:grid-cols-[.7fr_1.3fr] lg:gap-16"><SectionHeading eyebrow="Service FAQ" title={<>Everything you need to know <span className="text-brand-red">before starting.</span></>}/><Faq items={faqs}/></div></Container></section><FinalCta/></>}
